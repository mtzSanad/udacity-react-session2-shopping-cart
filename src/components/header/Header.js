import React from "react";
import "./Header.css";

//Header dumy component to render application navigation
const Header = () => {
  return (
    <nav className="header">
      <header>Shopping App</header>
    </nav>
  );
};

export default Header;
